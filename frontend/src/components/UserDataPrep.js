// import React from 'react';


// export const UserDataPrep = (items, Component) => {
//   const serialized = items && items.length ? items.map((item) => ({
//     id: item.id,
//     img: getUserAvatar(item), 
//     text: item.userName,
//     icon: options.active ? options.icon : options.inverseIcon,
//     onIconClick: options.onIconClick
//   })) : [];

//   return serialized.map((item) => {})
//     <Component />
// };

// export default UserDataPrep;